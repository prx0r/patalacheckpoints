# Marker — document → Markdown/JSON (difficult PDF fallback)

**What Pāṭala borrows:** converts documents to Markdown, JSON, chunks, or HTML — preserving equations
and references — and offers schema-guided structured extraction. A strong fallback for difficult PDFs
and rapid ingestion.

**License:** GPL-3.0 + model-specific (commercial-reuse caution). Repo: `datalab-to/marker`.

## How Pāṭala consumes it
**PLANNED.** Fallback in the paper-normalization substrate (`06-commentarial-graph.md`) when
Docling/GROBID struggle. NOT the irreplaceable core (license constraint).

## Doctrine
Use for rapid/difficult ingestion only; keep Docling + GROBID as the primary substrate.
