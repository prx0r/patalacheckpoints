# VedaWeb / Tekst — corpus search info architecture (watch)

**What Pāṭala borrows:** VedaWeb/Tekst's information architecture — text, token, annotation, lexical
link, metrical annotation, search facet, corpus position. VedaWeb supports morphologically + metrically
annotated text with lexicographic/corpus search; Tekst generalizes it as a collaborative research
platform for natural-language text resources.

**License:** varies. Repos: `VedaWebProject/vedaweb-legacy`, `VedaWebProject`.

## How Pāṭala consumes it
**WATCH.** Raid the information-architecture patterns (token/annotation/facet/corpus-position), NOT the
app. Also `viracitapada` (Sanskrit metrical-analysis).

## Doctrine
Adopt the patterns (text/token/annotation/lexical-link/metrical/search-facet), not the platform.
