# SESSION HANDOVER — 2026-08-12 (Agent 1 / ML lane)

*The complete record of this session's work: what was built, WHY (the decisions), what's real vs. hollow,
and the seed context a new agent needs. Read `AGENT1-HANDOVER.md` + `AGENTS-DOCTRINE.md` first — this is
the session-specific detail on top of that doctrine.*

---

## 0. THE THEME OF THE SESSION (why we did what we did)

We started by building ML layers (clustering, arguments, essays, gold chain), then an **external review +
self-audit revealed a recurring failure**: we kept building *structurally-elegant-but-hollow* objects and
reporting them as results. The session pivoted hard to **epistemic hardening** — the doctrine that
*"nothing is real because code exists; it's real only when independent gold + blind eval + metric + human
adjudication show it."* The last third of the session was applying that to the Nyāya gate.

---

## 1. WHAT WE BUILT (the full module inventory)

### The ML package (`machinelearning/research/patala_ml/`, 26 modules)
| Module | What it is | Honest status |
|---|---|---|
| `cluster.py` | hybrid-graph clustering → 9 V2/V3 theme proposals | real topology; MACHINE_PROPOSED |
| `c1corpus.py` | loads 63 C1 read/ files | infra |
| `argument.py` | Claim-v3 ArgumentProposal schema | **container, gate slot empty** |
| `strength.py` | BayesianEvidencePrimitive (weighted_lbf → Certainty) | math; uncalibrated (no epistemic role) |
| `gold.py` | ARG-GOLD-001 (hand-built V2-O argument) | the gold seed |
| `goldchain.py` + `philproof.py` | cross-layer certificate + L0 proof handshake | L0 floor real; upper layers MACHINE_PROPOSED |
| `nyayagate.py` | Pāṭala-adapted 5-hetvābhāsa gate | measured: detection 0.80, FP 0.00, abstain 0.50 |
| `aifgraph.py` | AIF proposition/inference/conflict graph | representation, no real content yet |
| `essaygen/essayplan/essaysentence/essayverify/essay` | the essay/synthesis layer | **scope creep** — essays are the endpoint, not the machine |
| `builders.py` + comparison | 4 alternative argument-builders | **RETIRED as CIRCULAR** (B-STRUCT was passage-title overlap) |
| `cleanup.py` | honest ID resolver (exact-match, no fuzzy) | real |
| `c1metrics.py` | C1 diagnostics (novelty/boundary/...) | thresholds unvalidated |
| `retrieval.py`, `eval.py`, `metrics.py` | BM25/dense/hybrid + eval harness | retrieval real (CP2) |
| `pushing.py` | parse PUSHING sessions | infra |
| `adjudicate.py` | human sign/promote loop | real mechanism |

### The benchmark (`benchmarks/v0/`, the most valuable thing)
- **MANIFEST / SCHEMA / SPLITS / METRICS** — frozen contracts (leakage S0–S4, per-metric, no aggregate)
- **ARG-GOLD-001** (`structure/PAT-STRUCT-001.json`) — the hand-built V2-O gold argument
- **retrieval fixtures** (`retrieval/PAT-RETRIEVAL-001.jsonl`)
- **Nyāya gate gold** (`evidence/nyaya-gate-gold.jsonl`, 12 fixtures) — added this session
- **runs/** — immutable run records (version/split/config/commit)

### Test totals (this session's final state)
**339 passing** (159 of which are the per-line re-baseline checks).

---

## 2. THE KEY DECISIONS (why we did what we did)

1. **Retired B-STRUCT / coverage-Spearman as CIRCULAR.** The "premises" were C1 titles; gt_overlap
   measured passage-title overlap, not reasoning. → The 4-builder comparison is invalid.
2. **Relabeled `strength.py`** from "truth-engine scorer" → `BayesianEvidencePrimitive` (hand-chosen
   weights = ordinal, not probability). Rejected porting the F1–F8/D1–D5/B1–B6 ontology (metaphysics-
   specific, doesn't fit Pāṭala).
3. **Fixed hardcoded `EDITOR_APPROVED`** in the gold chain — was fabricating review status. Now honest
   `MACHINE_PROPOSED` on all upper layers.
4. **Built the frozen benchmark** (CP0) — the evaluation substrate everything must route through.
5. **The Nyāya gate is the confirmed next real build** — it's real (680 LOC in truth-engine), unwired,
   and this session built its gold + a Pāṭala-adapted version + a blind eval.
6. **The doctrine is now enforced, not advisory** — AGENTS.md (auto-loads), README#0, docs/INDEX,
   theatre_check.py.

---

## 3. THE HONEST STATE (what's real vs. hollow — see CLAIMS.md)

**REAL:** the benchmark · the L0 proofs (other agent: V2/V3 35/35 P0 PASS, verified) · the Nyāya gate
(measured) · the clusterer topology · the gold-chain L0 floor.

**HOLLOW (admitted):** strength.py (uncalibrated) · argument.py (empty gate slot) · the essay layer ·
aifgraph (no real propositions) · c1metrics (unvalidated thresholds).

**The convergence:** the gold chain now consumes the **verified P0 proofs** (V2-L/O/P/S all pass, 0
unknown). The L0 floor is real; everything above is honestly MACHINE_PROPOSED.

---

## 4. THE NYĀYA GATE — WHERE WE LEFT IT (the active work)

- **Gold:** `benchmarks/v0/evidence/nyaya-gate-gold.jsonl` (12 fixtures, SINGLE_REVIEWED by one author)
- **Implementation:** `nyayagate.py` (Pāṭala-adapted 5-hetvābhāsa gate, no metaphysics content)
- **Blind eval** (`eval_gate_gold.py`): **detection 0.80 (4/5) · false-positive 0.00 (0/5) · abstention 0.50**
- **The hard remaining part:** the 1 miss is **viruddha** — detecting "memory proves the self is
  constructed" is a defect requires knowing the IPVV argues the OPPOSITE (V2-P). **Keyword rules cannot
  do this; it needs the argument graph.** That's the genuine integration of the gate + Pāṭala's C1/argument
  layer.
- **To promote to `verify-claim-semantic`:** viruddha-via-graph · independently reviewed gold (≥2
  reviewers) · abstention gap closed.

---

## 5. THE TRUTH-ENGINE ASSETS (the external goldmine — see TRUTHENGINE-FULL-AUDIT.md)

**Location:** `/root/projects/.meta/misc/truth-engine/` (code) + `docs-active/` (22 docs) — mirror at
`/root/projects/clean/`.

**The best assets:**
- **The Nyāya gate** (`scripts/nyaya-truthmap-gate.py`, 680 LOC) — real, deterministic, UNWIRED
- **TRUTHCHANGES6.md** — the definitive Nyāya spec (4 pramāṇas, 5 hetvābhāsas, tarka, 5-member syllogism)
- **RO-v2.md** (Research Object), **EO-v2.md** (Essay Object), **TRUTH-TEST.md**, **ENQUIRY-PIPELINE.md**
- **The F1–F8/D1–D5/B1–B6 ontology does NOT fit Pāṭala** — reuse mechanisms, reject ontology
  (`TRUTHENGINE_TO_PATALA_MAPPING.md`)

**The Lean/formal-proof work (`sanskritree/`):** mostly aspirational. It proves FOL tautologies + stipulated
definitions, not Abhinavagupta. `fol_lean_bridge.py` (vyāpti→∀, abheda→=) is real but the formal subset for
the IPVV is nearly empty. **Don't build on it** (`SANSKRITREE-LEAN-REVIEW.md`).

---

## 6. THE DOCTRINE (the governing rules — read these, they prevent the theater failure)

| File | What |
|---|---|
| `machinelearning/AGENTS-DOCTRINE.md` | the master rule: nothing real because code exists; 3 categories; 9-field contract; epistemic labels; banned words; abstention |
| `machinelearning/CLAIMS.md` | the project's self-audit ledger (P-001..P-008) |
| `machinelearning/COMPONENT-CONTRACTS.md` | the 9-field anti-theatre contract per component |
| `machinelearning/AGENT1-HANDOVER.md` | the working doctrine: axioms, recurring errors, tone, anti-theater |
| `machinelearning/theatre_check.py` | the mechanical gate — run before claiming anything is "done" |
| `machinelearning/dualagentvision.md` + `-ADAPTED.md` | the north star + checkpoint map (CP0–CP12) |
| `machinelearning/MLUSEINPATALA.md` | the frozen strategy + the north-star rule |

---

## 7. ARXIV PAPERS TO SEED CONTEXT (read to internalize the ML framing)

**Core retrieval / graph / provenance (verified, relevant):**
- **GraphRAG** — arXiv:2404.16130 (community summaries for global-theme questions)
- **ColBERTv2** — arXiv:2112.01488 (late-interaction retrieval, for Sanskrit terms)
- **RAPTOR** — arXiv:2401.18059 (multi-resolution recursive retrieval)
- **HippoRAG** — arXiv:2405.14831 (graph + Personalized PageRank multi-hop)
- **LightRAG** — arXiv:2410.05779 (dual-level graph/vector retrieval)

**The provenance / verification frontier (directly on-thesis):**
- **GenProve** — arXiv:2601.04932 (generation with sentence-level provenance: Quotation/Compression/Inference; Inference is the hard case)
- **TRACER** — arXiv:2605.09934 (verifiable generative provenance; verifier + RL reward)
- **PaperTrail** — arXiv:2602.21045 (claim-evidence interface; provenance lowers trust but doesn't change reliance)
- **Claimify** — arXiv:2502.10855 (claim extraction + decontextualization)
- **RAGChecker** — arXiv:2408.08067 (fine-grained retrieval/generation diagnosis)
- **MedRAGChecker** — arXiv:2601.06519 (atomic claims + NLI + KG consistency → /verify-claim-semantic)

**Argument / formalization:**
- **Lean Copilot** — arXiv:2404.12534 (LLM in Lean — but see the caveat: proves formal subset, not Abhinavagupta)
- Argument Mining (ABAM 2011.00633, Multilingual 2010.06432) — for premise extraction

**The full verified curriculum:** `machinelearning/mlcurriculum.md` (26 papers, all IDs confirmed).

---

## 8. THE KEY FILES TO TOUCH NEXT (Agent 1)

1. `nyayagate.py` + `eval_gate_gold.py` — the active work: viruddha-via-graph
2. `argument.py` — fill the empty `gate` slot with the real gate
3. `lib/verify.ts` — add `verifyClaimSemantic` (the API surface)
4. `benchmarks/v0/` — grow gold (independent review; more fixtures)
5. `pipeline/` — the gate integration + `/api/verify/claim-semantic`

---

## 9. THE ONE-PARAGRAPH CARRY-FORWARD

We built a full ML derivation spine (cluster→argument→essay→gold-chain), then an audit exposed that most
of it was structurally-elegant-but-hollow. We pivoted to epistemic hardening: a frozen benchmark, an
enforced doctrine (AGENTS.md + theatre_check.py), honest relabeling, and one genuinely measured result —
the Nyāya gate detecting 4/5 defect types with zero false positives on a small blind gold. The
convergence is real: the gold chain now bottoms out in verified P0 proofs. The immediate next work is the
one genuinely hard piece: **viruddha detection via the argument graph** — where the Nyāya gate and
Pāṭala's C1/argument layer finally connect for real. Everything else is either done (benchmark), real-but-
unwired (gate), or infrastructure awaiting validated content (the rest). Don't build new layers; fill the
real ones and route every claim through the benchmark.

---

## SESSION UPDATE (2026-08-12, later) — vertical object + review-driven hardening

**Built (CP4, empirical):**
- ARG-GOLD-001..005 built + `validate_gold`-consistent (ARG-003 reductio, ARG-004 conceptual-distinction,
  ARG-005 interpretive-scope).
- Build 4: primitive extractor run BLIND vs the 5 golds → immutable BenchmarkRun
  (`benchmarks/v0/runs/2026-08-12T124709Z/`): lexical-overlap F1 0.36, inference recovery 0.0 (the floor).
- **Gate #3 — the VERTICAL OBJECT**: `patala_ml/vertical.py` serializes ARG-001 G-TC2 all the way down
  (ResearchQuestion→…→PhilologicalProof), every arrow resolved to real L0/source/proof data
  (`benchmarks/v0/vertical/vertical-v2o-g-tc2.json`). This is the working miniature of the vision.
- Review-driven: golds carry task_level A/B/C, candidate_reconstruction, support_scope; the validator
  now checks structural well-formedness only; the IR→graph mapping is marked NOT FROZEN.

**Honest status:** the 5 golds are `MACHINE_PROPOSED/CANDIDATE` (not independently reviewed). The vertical
object resolves mechanically but its term→anchor mapping is machine-proposed. CLAIMS: P-003 NOT_ESTABLISHED,
P-014 vertical object SUPPORTED-as-infrastructure.

**Next:** (1) independent review of the 5 golds (ARG-GOLD-REVIEW-PROTOCOL); (2) compile one vertical object
to py-aspic (first external evaluator); (3) a real extractor beating the baseline.

---

## SESSION UPDATE (2026-08-12, later) — Vertical Object v0 HARDENED + frozen

Applied the review's six bounded corrections (no new rabbit hole):
- GOLD grounding now uses EXACT L0 refs (grounding_refs) — no substring search for gold. Term search
  demoted to `candidate_context` (discovery only).
- Every edge is a typed `GroundingLink` {from, to, relation, resolution, review_state}.
- Proof resolution is REAL: on-disk artifact marked STALE (predates frozen 35/35 P0), NOT "resolved".
- C1/L2 at SPAN_LEVEL with exact spans (manual for this case); else DOCUMENT_LEVEL.
- Proposition role (PREMISE/CONCLUSION) recorded per inference.
- Missing IR fields (research_question/commitment/task_level on ARG-001) surfaced, not retrofitted.
- Test renamed in spirit to resolution/integrity tests (0 fail); Vertical Object v0 FROZEN.

**Next (per review):** independent gold review first; then py-aspic ONLY over the cleanest reviewed
argument (adapter/semantics test; provenance stays in Pāṭala). Not ARG-003 (contentious regress) /
ARG-001 (essence unclear) until reviewed.
