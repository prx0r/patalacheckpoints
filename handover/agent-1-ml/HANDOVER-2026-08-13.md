# AGENT 1 — HANDOVER (2026-08-13) — to the next agent continuing the scholar-corpus foundation

*The durable handover for whoever takes up the Agent 1 lane next. Read `AGENTS.md` + `AGENTS-DOCTRINE.md` first.
This captures: the frozen ML vertical, the pivot to the scholar-corpus foundation (S0), the current state, the
plan, the git situation, and exactly what to do next.*

---

## 1. WHO YOU ARE / WHERE YOU ARE

You are **Agent 1 (ML / research + the scholar-evidence foundation)**. The original ML lane is **frozen** — its
Pāṭala Review vertical (synthesis → EO → essay → SentenceEvidenceAudit + k-core/Louvain) is done, validated, and
**peer-review-clean relative to the current objects**. The forward work is the **scholar-corpus foundation (S0)**:
turning the on-disk published-scholar corpus (Sanderson/Ratié/Torella/Bäumer) into a provenance-addressable
evidence corpus that corroborates Pāṭala's propositions — **without needing a live human reviewer**.

## 2. THE PIVOT (why the plan changed)

- **The scholar corpus IS the human gate.** We own thousands of pages of published Sanskrit scholarship. It is a
  **scalable scholarly corroboration oracle** (can promote matched claims to `SCHOLARLY_CORROBORATED` without a
  live reviewer). It is **NOT** review/adjudication of Pāṭala's exact object.
- **Reuse-first.** Do NOT build scholarly infra (PDF parsing, bibliography, RAG, annotation UI, benchmark
  framework). Borrow mature open tools; Pāṭala owns only the **epistemic dependency graph**:
  `SourceAssertion · EvidenceUse · CorroborationEvent · SemanticAlignment · Proposition · Commitment · DebateFrame ·
  EpistemicRegime · InferenceApplication · Argument · Attack · Crux · ArgumentSynthesis · ReviewEvent ·
  ImpactReport · epistemic ceiling · dependency propagation · staleness/supersession` + Pāṭala-specific benchmark
  scorers.
- The vision is not a mammoth build — it is **removing everything that isn't the vision** (the epistemic graph).

## 3. CURRENT STATE (honest)

**DONE / FROZEN:**
- The Pāṭala Review vertical (`0efc1df` → `b1fb034` + review passes → `aef17dd`) — peer-review-clean relative to
  the current objects.
- `P-019 v2` deterministic k-core/Louvain structural baseline (`7ea182c` + `76263d8`).
- **S0 source-evidence substrate** scaffolded (`source-evidence/`): the Profile schema (FaBiO/PROV/Web
  Annotation/CiTO/RO-Crate/IIIF composition), a registry seeded from the Sanderson manifest + `.meta`, an RO-Crate
  emitter + a 5-file pilot, the reuse-first doctrine, the integration spec, the bibliography reconciliation spec,
  and **27 per-tool docs**.

**REAL / NOT YET (the forward work):**
- S0.1 external-tool pilot (GROBID/Zotero/Crossref/OpenAlex adapters + the resolver) — only the schema + RO-Crate
  pilot exist.
- S0.2 the missing semantics validators (SourceAssertion/CorroborationEvent).
- S0.3 product proof (one proposition resolves in bibliography/assistant/argument/site/education).
- F1 false-positive-tested scholarly corroboration.
- The **Inspect AI** prototype (the immediate next step).

**NOT ESTABLISHED (honestly):** automatic argument reconstruction (0.36/0.0) · accepted themes · a generalized
essay/philosophy engine · `INDEPENDENT_REVIEWED` (the one state we can't reach — no longer the gate).

## 4. THE PLAN (see `machinelearning/_ACTIVE/DEVPLAN.md` rev 5.x + `source-evidence/docs/`)

```
S0.0 freeze the contract (RawSource→BibliographicRecord→Witness→SourceSpan→SourceAssertion→CorroborationEvent→consumer)
S0.1 external-tool pilot (GROBID/Zotero/Crossref/OpenAlex + the thin resolver; LIVE/RECORDED/UNAVAILABLE rule)
S0.2 missing semantics (SourceAssertion + CorroborationEvent validators; adversarial tests)
S0.3 product proof (one proposition resolves across 5 consumers)
S0.4 F1 corroboration experiment (false-positive-tested) → CorroborationBench → TantraFact
```

**The ruthless 8-experiment order** (each asks "does this delete a subsystem we planned to write?"):
Inspect AI → PaperQA2 → INCEpTION → Recogito → STORM/Co-STORM → COAR Notify (doc only) → Manubot → RAiD/credit.

## 5. GIT STATE (critical)

- **Canonical line: `origin/agent1-argument-layer-a1b`.** Use this as the source of truth.
- `origin/agent1-argument-layer` is stuck at Commit A (pre-A.1) due to a history fork — **Agent 0 reconciliation**
  (`handover/agent-1-ml/GIT-RECONCILIATION-2026-08-12.md`). Do NOT surgery/force-push.
- The local worktree on `/mnt/HC_Volume_106427611` is **unstable** (files/branch pointers periodically revert).
  Re-restore files from the a1b branch after any suspected revert; commit via a fresh clean worktree
  (`git worktree add /tmp/opencode/a1b-wt origin/agent1-argument-layer-a1b`, copy changed files, commit, push,
  remove the worktree).
- Commit path used: `git worktree add <tmp> origin/agent1-argument-layer-a1b` → copy files → `git add` your paths →
  `git commit` → `git push origin HEAD:agent1-argument-layer-a1b` → `git worktree remove --force`.

## 6. THE DOC MAP (read these)

- `VISION_AND_NAVIGATION.md` (the compass) + `docs/vision/INDEX.md` (incl. the vision→tools map)
- `machinelearning/_ACTIVE/DEVPLAN.md` (rev 5.x — the authoritative plan) + `handover/agent-1-ml/NEXT-STEPS.md` (rev 6)
- `machinelearning/_ACTIVE/CURRENT-STATE-ARGUMENT-LAYER.md` (the frozen vertical)
- `docs/global/GLOBAL-STATE-2026-08-13.md` (timestamped global checkpoint, incl. the CURRENT DIRECTION section)
- **`source-evidence/docs/`** (the S0 heart):
  - `tool-integration.md` (THE authoritative S0 execution guide) · `tool-integration2.md` (expanded) ·
    `tool-integration3.md` (final architecture)
  - `reuse-first-stack.md` · `scholar-layer-schema-stack.md` · `scholar-layer-evaluation-and-ids.md`
  - `INTEGRATION-SPEC.md` (tools → current Pāṭala modules) · `bibliography-reconciliation-spec.md`
  - `tools/` (27 per-tool docs, incl. polite rate-limiting)
- `handover/agent-1-ml/AGENT1-CHECKPOINT-2026-08-13.md` (the red-team closeout of the ML vertical)

## 7. GUARDRAILS

1. Reuse before building — the criterion: "does a mature open tool already solve this?" (GROBID/Zotero/OpenAlex/
   OpenCitations/RO-Crate/PaperQA2/Inspect/INCEpTION/Recogito/…). Spend Pāṭala engineering only where the epistemic
   graph begins.
2. Corroboration is proposition-by-proposition via `CorroborationEvent`s; preserve disagreement
   (`SCHOLARLY_CONTESTED`); track independence (never raw source count); `UNKNOWN` rights is valid.
3. External metadata is a WITNESS, never an authority; canonical decisions are Pāṭala's and deterministic;
   audited (`verified:true`) records are immutable against external metadata.
4. The evaluation plane sits OUTSIDE the production graph; TantraFact must not be generated from the graph it
   evaluates.
5. Join on `pt:*` IDs — never fuzzy. Route everything through `benchmarks/v0/` + record a `BenchmarkRun`.
6. Do NOT reopen the frozen argument-pack architecture or the ML vertical; do NOT port the Bayesian ontology.

## 8. THE ONE-SENTENCE CARRY-FORWARD

**The ML vertical is closed and peer-review-clean; the forward work is the scholar-corpus foundation — build
`source-evidence/` by borrowing mature open tools (GROBID/Zotero/OpenAlex/OpenCitations/RO-Crate/PaperQA2/Inspect/
INCEpTION/Recogito) and adding only the epistemic dependency graph (SourceAssertion → CorroborationEvent →
Proposition → Argument → Crux → Synthesis → ReviewEvent → dependency), so the on-disk published scholars become a
provenance-addressable corroboration oracle that needs no live reviewer. Start with the Inspect AI prototype (port
one existing benchmark + the laundering mutations into an Inspect task).**
