# AGENT 2 — SESSION HANDOVER (2026-08-12) — for the next Agent 2

*The complete record of this session's work + the honest current state + the highest-value next work.
Read `CHECKPOINTS-INTEGRATION.md` + `INDEX.md` + this doc. Append to, never overwrite.*

---

## THE ONE-LINE STATE

**Pāṭala now has a working RAW-L0 factory core (raw Sanskrit → audited canonical L0, validated by the
existing verify_l0.p0_proof), a prioritized 21-target + 39-lead translation queue, a versioned L0
registry, and the full sanskritree goldmine consolidated into a queryable DB + import manifest. The
autonomous translator is structurally ready; the gloss/generative layer + a reliable model transport is
the remaining gap.**

---

## WHAT AGENT 2 IS (the role)

**Corpus Compiler + Integrity Layer** (not "the L0 agent"). Owns: `data/corpus/`, `app/`, `lib/`,
`pipeline/` (verify_l0, corpus_state, raw_l0, agent3_batch, agent3_queue, l0_registry, review_engine),
`handover/agent-2-integration/`. Questions: *is this reading licensed by the source? what do we have,
where, what state, can every artifact resolve?* Does NOT generate translations (Agent 3), write C1 /
choose interpretive readings (Agent 1), promote machine output.

---

## THE GOLDEN RULES (from the doctrine, non-negotiable)

1. **A wrong translation is worse than none.** Validation is the hard gate — never let the factory
   outrun the validator. P0 lossless + false-certainty + abstention + chunk review.
2. **Proof dimensions stay separate** (source_span PROVED · segmentation/morphology SUPPORTED ·
   lexical_sense MACHINE_PROPOSED). Never collapse into `confidence: .93`.
3. **L0 is immutable + versioned** ("never edit in place — a fix emits a new version").
4. **Hermes is Pāṭala's replaceable execution kernel, NOT its epistemic backend.** Hermes never
   determines what Pāṭala knows.
5. **Git discipline:** stage only your own paths, verify branch (agent2), never surgery on a dirty
   shared tree. Agent 1's work is on `agent1`; a `62cf778` Agent-1 commit sits on `agent2` pending
   Agent-0 reconciliation.
6. **Translation targets are prioritized** (Krama packet first) per the expansion docs.

---

## THE BUILD STATE (what exists, tested)

### The autonomous factory core (this session's main work)
| Piece | File | Status |
|---|---|---|
| RAW-L0 (raw Sanskrit → canonical L0) | `pipeline/raw_l0.py` | ✅ Vidyut segmentation/lemma/morph + P0 proof; verse locators stripped as structural; lemma=null → AMBIGUOUS |
| The batch runner | `pipeline/agent3_batch.py` | ✅ verses → RAW-L0 → audit → ledger update → manifest |
| The work queue | `pipeline/agent3_queue.py` | ✅ 21 prioritized targets, processes next passage, resume-after-failure |
| The versioned L0 registry | `pipeline/l0_registry.py` | ✅ immutable versions (v1/v2/...) + commit + mark_reviewed |
| The prioritized targets | `pipeline/translation_targets.py` | ✅ 21 targets (Krama first) + 39 leads |
| The pipeline audit (easy wins) | `pipeline/audit_translation_pipeline.py` | ✅ 40 works with existing T1/R1/T2/R3/C1 |
| The corpus-targets DB | `pipeline/build_corpus_targets_db.py` | ✅ sources/targets/leads/anchors/index |
| The review engine (executable corrections) | `pipeline/review_engine.py` | ✅ Phase 3A+3D (the moat): append-only ReviewEvent → reducer → ImpactReport; MCP tools |

### Verified results
- RAW-L0 on a Kramasadbhāva verse → canonical 15-field records, P0 PASS, 0 unknown, honest AMBIGUOUS.
- Queue lists 12 RAW-Sanskrit works actionable now (Krama packet first); 21 targets + 39 leads tracked.
- L0 registry: kramasadbhava v1-v4 immutable, mark_reviewed works.
- Review engine: a REVISE on G2-TC2 → G2-INF1/G2-CONC NEED_REVIEW, ARG-004 untouched, idempotent.

---

## THE GOLDMINE (consolidated this session — read before translating)

- **`docs/corpus/TARGETS-INDEX.md`** — the master index (DB + the two goldmine docs).
- **`docs/corpus/SANSKRITREE-IMPORT-MANIFEST.md`** — the full sanskritree audit + where things import.
- **`canonical_reference_map.md`** (sanskritree/corpus/targets/) — taxonomy, ingestion waves, **the
  semantic-shift glossary** (the fix for cross-work term misreading), auditable architecture.
- **`markguidance.md`** — the Recognition Enquiry: passage dossiers, the A/B/C thesis levels (for Agent 1).
- `data/atlas/concepts.ts` — 25 concepts (completed with śakti + pratyabhijñā).

---

## THE HONEST GAPS (the next Agent 2's work, in priority order)

1. **The gloss/generative layer.** `literal_gloss` is empty in deterministic mode; it needs a working
   model transport. hermes is currently hanging/unreliable on this box (403 / hangs). The deterministic
   core doesn't need it, but the L0 isn't complete without the gloss. **This is the top gap.**
2. **Sanskrit-only replay benchmark** (the northstar Build 3): hide IPVV gold English, run RAW-L0, compare
   against gold → measures segmentation/lemma/morphology/gloss/abstention/false-certainty. The embryo of
   Pāṭala Evals.
3. **Not-yet-ingested primary texts** (from the import manifest) → `data/corpus/passages/`.
4. **Cross-work L0 generalization** — Kramasadbhāva first (already RAW_SANSKRIT + prioritized #1).
5. **Bibliography linkage** — defer until a work reaches a real C1/live milestone.
6. **P2 blind review** (160 cases) → human-validated (non-blocking).

---

## WHAT NOT TO DO (guardrails)

- Don't build more review UX (3E/3F) until a real reviewer is ready.
- Don't import the Lean/Pantograph code as a working capability (aspirational).
- Don't import `syntheses/*` or the mystical `truth/` dirs (noise).
- Don't re-ingest already-captured corpus (kubjikāmata, maharthamanjari, ipvv).
- Don't let Hermes determine what Pāṭala knows.

---

## CROSS-LANE (for the coordinator / Agent 1)

- **Flag:** commit `62cf778` (Agent 1 ML work) sits on `agent2` — reconcile to `agent1`.
- **For Agent 1:** the argument-layer gold in `truth/` (TANTRALOKA_MASTER, sambandhasiddhi, apoha,
  dharmakīrti) is the imported substrate for CP4 Argument Gold. `markguidance.md` = the Recognition
  dossiers. The review engine (executable corrections) is ready to validate Agent 1's proposed objects.

---

## THE ONE-SENTENCE CARRY-FORWARD

**Agent 2 has built the RAW-L0 factory core, the prioritized translation queue, the versioned L0
registry, the executable-corrections review engine, and consolidated the full sanskritree goldmine into a
queryable DB — so the autonomous translator is structurally ready; the next Agent 2 should (1) wire a
reliable gloss/model transport, (2) run the Sanskrit-only replay benchmark against IPVV gold, and
(3) ingest the not-yet-ingested primary texts — while holding the doctrine's hard line that validation is
the gate, L0 is immutable/versioned, and a wrong translation is worse than none.**
