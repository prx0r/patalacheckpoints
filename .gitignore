node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# large generated data files (regenerate, don't commit)
data/manuscripts.json
data/corpus/passages/kubjikamata.jsonl
__pycache__/
*.pyc

# local ML virtualenv (5GB torch/nvidia) — the research environment, NOT part of the app
machinelearning/research/.venv/

# Inspect AI eval logs (regenerated on each run; don't commit)
logs/

# Atlas dev virtualenv + local env
.venv-atlas/

# local secrets (R2/Cloudflare credentials — NEVER commit)
.env.local
.r2-env
